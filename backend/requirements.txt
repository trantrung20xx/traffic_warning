fastapi
# Framework xây dựng API backend cho hệ thống

starlette
# FastAPI phụ thuộc vào Starlette, và code hiện import trực tiếp WebSocketState từ starlette

uvicorn[standard]
# ASGI server dùng để chạy ứng dụng FastAPI

opencv-python
# Thư viện xử lý ảnh và video (đọc frame từ camera, xử lý hình ảnh)

numpy>=1.24,<2.4
# Thư viện tính toán số học, xử lý ma trận (dùng trong xử lý ảnh)

shapely
# Thư viện geometry/spatial mature dùng cho point-in-polygon, line intersection và các phép hình học ổn định hơn

pydantic
# Định nghĩa schema dữ liệu và validate dữ liệu đầu vào/đầu ra API

pytest
# Bộ test runner cho backend

SQLAlchemy
# ORM dùng để thao tác với cơ sở dữ liệu (SQLite)

#pydantic-settings
# Quản lý cấu hình ứng dụng (biến môi trường, config hệ thống)

#sqlalchemy-utils
# Thư viện bổ sung tiện ích cho SQLAlchemy

ultralytics
# Thư viện chứa mô hình YOLOv8 dùng để phát hiện phương tiện

# PyTorch được cài riêng tùy theo môi trường phần cứng:
# CPU:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# GPU NVIDIA (ví dụ CUDA 13.0):
#   pip install torch==2.10.0 torchvision==0.25.0 --index-url https://download.pytorch.org/whl/cu130

# Inference backend mặc định (pytorch):
torch
torchvision
# Ultralytics phụ thuộc PyTorch để chạy mô hình .pt
# Inference backend thay thế (được source hỗ trợ qua `detection.backend`):
# onnxruntime
# openvino; platform_system == "Windows" or platform_system == "Linux"
# tensorrt==10.16.1.11; platform_machine == "AMD64" or platform_machine == "x86_64"
# Lưu ý:
# - TensorRT chỉ nên dùng trên máy có NVIDIA driver/CUDA tương thích.
# - Backend thực tế dùng backend nào phụ thuộc vào `detection.backend` và định dạng weight
#   (.pt/.onnx/.xml/.engine), không tự đổi hành vi nhận diện nếu bạn vẫn để backend = pytorch.

lap>=0.5.12
# Thư viện phục vụ thuật toán ByteTrack (matching bounding box)
# Cài sẵn để tránh việc Ultralytics tự tải khi chạy lần đầu

# Optional but commonly useful for better decoding/robust RTSP:
# ffmpeg-python==0.2.0
# Thư viện hỗ trợ xử lý luồng video (RTSP) ổn định hơn

python-multipart
# Xử lý upload file (form-data) trong FastAPI

openpyxl
# Thư viện dùng để tạo và xuất file Excel (.xlsx)

# License plate OCR backend mặc định:
paddleocr
paddlepaddle==3.2.2
easyocr

# Nếu chạy GPU, có thể thay `paddlepaddle` bằng `paddlepaddle-gpu` theo môi trường triển khai.
